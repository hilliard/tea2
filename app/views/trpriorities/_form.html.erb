<%= form_for(@trpriority) do |f| %>
  <% if @trpriority.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trpriority.errors.count, "error") %> prohibited this trpriority from being saved:</h2>

      <ul>
      <% @trpriority.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container">
  <div class="field">
    <%= f.label "Priority" %><br>
    <%= f.text_field :trpriorityname %>
  </div>
  <div class="actions">
    <%= f.submit("Create") %>
  </div>
<% end %>
</div>
